import type { AppPlugin } from '@/core/plugin'
// import { exampleOverridePlugin } from '@/plugins/example.override'

// Register custom plugins here.
// Order matters: later plugins can override module slots from earlier plugins.
export const userPlugins: AppPlugin[] = [
  // exampleOverridePlugin,
]
